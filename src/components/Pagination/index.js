import { View, Text } from 'react-native'
import React from 'react'
import { GlobalText } from '../../assets/globalStyle'

const Pagination = () => {
    return (
        <View>
            <GlobalText>TODO -- Pagination</GlobalText>
        </View>
    )
}

export default Pagination