import { View, Text } from 'react-native'
import React from 'react'
import { GlobalText, MainView } from '../../../assets/globalStyle'

const Progress = () => {
  return (
    <MainView>
      <GlobalText>Progress</GlobalText>
    </MainView>
  )
}

export default Progress